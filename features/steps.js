const { Given, When, Then } = require("@cucumber/cucumber");

Given('a customer has already an account with Mcafee', function () {
    
    
});


When('I pass the {string}', function (string) {
           
          
});

Then('the active {string} details of the customer is displayed', function (string) {
   
   
});